# deposit
# no bag-store.bag-name, not set
# no creation.timestamp, defaults to creationTime
# no depositor.userId, asked via interactor

# state
# no state.label, asked via interactor
# no state.description, asked via interactor

# ingest step
# no deposit.ingest.current-step, not set

# identifiers
# no identifier.doi, asked via interactor
# no identifier.urn, asked via interactor
# no identifier.fedora, asked via interactor
# no bag-store.bag-id, defaults to depositId

# doi events
identifier.dans-doi.registered = yes
# no identifier.dans-doi.action, defaults to CREATE

# curator
# no curation.datamanager.userId, not set
# no curation.datamanager.email, not set

# curation events
# no curation.is-new-version, not set
# no curation.required, not set
# no curation.performed, not set

# springfield
# no springfield.domain, not set
# no springfield.user, not set
# no springfield.collection, not set
# no springfield.playmode, not set

# content-type
# no easy-sword2.client-message.content-type, not set
