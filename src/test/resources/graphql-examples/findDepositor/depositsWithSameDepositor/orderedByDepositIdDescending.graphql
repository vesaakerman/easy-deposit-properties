query ListDepositsFromDepositor {
    depositor(id: "user002") {
        deposits(orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            id
            state {
                label
                description
            }
        }
    }
}
