query {
    deposits(doiRegistered: {label: "yes", filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        edges {
            node {
                depositId
                doiRegistered
            }
        }
    }
}
