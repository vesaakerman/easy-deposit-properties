query {
    deposits(doiRegistered: {label: "yes", filter: LATEST}, orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        edges {
            node {
                depositId
                doiRegistered
            }
        }
    }
}
