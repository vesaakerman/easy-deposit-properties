query {
    deposits(doiRegistered: {label: "yes", filter: LATEST}, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        edges {
            node {
                depositId
                doiRegistered
            }
        }
    }
}
