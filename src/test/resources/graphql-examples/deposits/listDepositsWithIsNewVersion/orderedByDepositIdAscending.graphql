query {
    deposits(isNewVersion: {value: true, filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        edges {
            node {
                depositId
                isNewVersionEvents {
                    isNewVersion
                    timestamp
                }
            }
        }
    }
}
