query {
    deposits(isNewVersion: {value: false, filter: LATEST}) {
        edges {
            node {
                depositId
                isNewVersionEvents {
                    value
                    timestamp
                }
            }
        }
    }
}
