query {
    deposits(isNewVersion: {value: true, filter: LATEST}) {
        edges {
            node {
                depositId
                isNewVersionEvents {
                    isNewVersion
                    timestamp
                }
            }
        }
    }
}
