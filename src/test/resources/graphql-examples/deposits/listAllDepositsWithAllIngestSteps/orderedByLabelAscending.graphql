query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            depositId
            ingestSteps(orderBy: {field: STEP, direction: ASC}) {
                nodes {
                    step
                    timestamp
                }
            }
        }
    }
}
