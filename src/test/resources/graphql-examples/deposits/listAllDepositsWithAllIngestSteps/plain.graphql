query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        edges {
            node {
                depositId
                ingestSteps {
                    edges {
                        node {
                            step
                            timestamp
                        }
                    }
                }
            }
        }
    }
}
