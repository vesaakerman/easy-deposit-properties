query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            depositId
            ingestSteps {
                nodes {
                    step
                    timestamp
                }
            }
        }
    }
}
