query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            depositId
            ingestSteps(orderBy: {field: TIMESTAMP, direction: DESC}) {
                nodes {
                    step
                    timestamp
                }
            }
        }
    }
}
