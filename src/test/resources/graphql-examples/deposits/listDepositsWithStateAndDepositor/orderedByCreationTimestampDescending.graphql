query ListDepositsWithStateAndDepositor {
    deposits(state: ARCHIVED, depositorId: "user001", orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
