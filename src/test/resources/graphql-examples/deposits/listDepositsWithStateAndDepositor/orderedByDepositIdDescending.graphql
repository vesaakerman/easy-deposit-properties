query ListDepositsWithStateAndDepositor {
    deposits(label: ARCHIVED, depositorId: "user001", orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
