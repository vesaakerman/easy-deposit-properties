query ListDepositsWithStateAndDepositor {
    deposits(state: ARCHIVED, depositorId: "user001", orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        nodes {
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
