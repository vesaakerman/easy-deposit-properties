query ListDepositsWithStateAndDepositor {
    deposits(label: ARCHIVED, depositorId: "user001", orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
