query ListDepositsWithStateAndDepositor {
    deposits(state: ARCHIVED, depositorId: "user001", orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
