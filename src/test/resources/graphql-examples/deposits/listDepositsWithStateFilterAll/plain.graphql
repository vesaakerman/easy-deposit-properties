query ListDepositsWithState {
    deposits(state: DRAFT, stateFilter: ALL) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
