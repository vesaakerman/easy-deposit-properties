query ListDepositsWithState {
    deposits(state: {label: DRAFT, filter: ALL}) {
        nodes {
            depositId
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
