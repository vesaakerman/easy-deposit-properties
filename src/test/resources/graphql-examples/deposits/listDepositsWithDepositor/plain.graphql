query ListDepositsFromDepositor {
    deposits(depositorId: "user002") {
        nodes {
            depositId
            state {
                label
                description
            }
        }
    }
}
