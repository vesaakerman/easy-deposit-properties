query ListDepositsFromDepositor {
    deposits(depositorId: "user002", orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        id
        state {
            label
            description
        }
    }
}
