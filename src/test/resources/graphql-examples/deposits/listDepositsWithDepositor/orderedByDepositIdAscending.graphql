query ListDepositsFromDepositor {
    deposits(depositorId: "user002", orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        state {
            label
            description
        }
    }
}
