query ListDepositsFromDepositor {
    deposits(depositorId: "user002", orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        nodes {
            depositId
            state {
                label
                description
            }
        }
    }
}
