query ListDepositsFromDepositor {
    deposits(depositorId: "user002", orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        id
        state {
            label
            description
        }
    }
}
