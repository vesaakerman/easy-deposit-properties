query ListDepositsWithState {
    deposits(ingestStep: {label: VALIDATE, filter: ALL}) {
        nodes {
            depositId
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                label
            }
        }
    }
}
