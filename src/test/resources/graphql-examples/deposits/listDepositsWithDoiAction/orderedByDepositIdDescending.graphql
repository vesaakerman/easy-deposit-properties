query {
    deposits(doiAction: {value: CREATE, filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        edges {
            node {
                depositId
                doiAction
            }
        }
    }
}
