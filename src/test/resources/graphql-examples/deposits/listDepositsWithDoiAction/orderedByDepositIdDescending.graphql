query {
    deposits(doiAction: {label: "create", filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        edges {
            node {
                depositId
                doiAction
            }
        }
    }
}
