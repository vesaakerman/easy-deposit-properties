query {
    deposits(doiAction: {value: CREATE, filter: LATEST}, orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        edges {
            node {
                depositId
                doiAction
            }
        }
    }
}
