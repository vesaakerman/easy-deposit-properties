query {
    deposits(doiAction: {label: "create", filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        edges {
            node {
                depositId
                doiAction
            }
        }
    }
}
