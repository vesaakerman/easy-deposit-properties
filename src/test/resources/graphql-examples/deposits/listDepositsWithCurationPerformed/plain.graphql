query {
    deposits(curationPerformed: {value: true, filter: LATEST}) {
        edges {
            node {
                depositId
                curationPerformedEvents {
                    value
                    timestamp
                }
            }
        }
    }
}
