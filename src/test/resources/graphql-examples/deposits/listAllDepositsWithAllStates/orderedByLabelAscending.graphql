query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        states(orderBy: {field: LABEL, direction: ASC}) {
            label
            description
            timestamp
        }
    }
}
