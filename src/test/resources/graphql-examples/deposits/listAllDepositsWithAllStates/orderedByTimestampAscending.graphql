query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            depositId
            states(orderBy: {field: TIMESTAMP, direction: ASC}) {
                nodes {
                    label
                    description
                    timestamp
                }
            }
        }
    }
}
