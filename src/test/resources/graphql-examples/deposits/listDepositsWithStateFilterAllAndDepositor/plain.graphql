query ListDepositsWithStateAndDepositor {
    deposits(state: DRAFT, stateFilter: ALL, depositorId: "user001") {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
