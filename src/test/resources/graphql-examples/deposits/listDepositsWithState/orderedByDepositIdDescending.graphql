query ListDepositsWithState {
    deposits(label: ARCHIVED, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
