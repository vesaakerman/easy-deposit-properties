query ListDepositsWithState {
    deposits(label: ARCHIVED) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
