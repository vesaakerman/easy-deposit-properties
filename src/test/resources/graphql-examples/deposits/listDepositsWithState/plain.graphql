query ListDepositsWithState {
    deposits(state: ARCHIVED, stateFilter: LATEST) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
