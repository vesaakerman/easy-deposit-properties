query ListDepositsWithState {
    deposits(state: ARCHIVED, stateFilter: LATEST, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        nodes {
            depositId
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
