query ListDepositsWithState {
    deposits(label: ARCHIVED, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
