query ListDepositsWithState {
    deposits(state: ARCHIVED, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
