query ListDepositsWithState {
    deposits(state: ARCHIVED, stateFilter: LATEST, orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
