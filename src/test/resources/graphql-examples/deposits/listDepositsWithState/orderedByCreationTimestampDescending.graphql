query ListDepositsWithState {
    deposits(state: ARCHIVED, orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
