query ListDepositsWithState {
    deposits(label: ARCHIVED, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
