query ListDepositsWithState {
    deposits(state: ARCHIVED, stateFilter: LATEST, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        nodes {
            depositId
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
