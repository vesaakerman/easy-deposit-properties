query ListDepositsWithState {
    deposits(state: ARCHIVED, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        id
        creationTimestamp
        depositor {
            depositorId
        }
        state {
            description
        }
    }
}
