query {
    deposits(contentType: {value: ZIP, filter: LATEST}, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        edges {
            node {
                depositId
                contentType {
                    value
                    timestamp
                }
            }
        }
    }
}
