query {
    deposits(contentType: {value: ZIP, filter: LATEST}, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
        edges {
            node {
                depositId
                contentType {
                    value
                    timestamp
                }
            }
        }
    }
}
