query ListDepositsWithState {
    state(label: SUBMITTED) {
        deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
            id
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
