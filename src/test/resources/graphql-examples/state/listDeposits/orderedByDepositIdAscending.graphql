query ListDepositsWithState {
    state(label: ARCHIVED) {
        deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
            id
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
