query ListDepositsWithState {
    state(label: ARCHIVED) {
        deposits(orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            id
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
