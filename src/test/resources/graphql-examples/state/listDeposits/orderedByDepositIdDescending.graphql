query ListDepositsWithState {
    state(label: SUBMITTED) {
        deposits(orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            id
            creationTimestamp
            depositor {
                depositorId
            }
            state {
                description
            }
        }
    }
}
