query ListDepositsFromDepositor {
    depositor(depositor: "user002") {
        deposit(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
            id
            state {
                label
                description
            }
        }
    }
}
