query ListDepositsFromDepositor {
    depositor(depositor: "user002") {
        deposit(orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            id
            state {
                label
                description
            }
        }
    }
}
