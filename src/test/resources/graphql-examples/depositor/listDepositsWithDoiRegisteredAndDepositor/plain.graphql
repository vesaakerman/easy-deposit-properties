query {
    depositor(id: "user001") {
        deposits(doiRegistered: {value: true, filter: LATEST}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
