query ListDepositsFromDepositor {
    depositor(id: "user002") {
        deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
            nodes {
                depositId
                state {
                    label
                    description
                }
            }
        }
    }
}
