query {
    depositor(id: "user001") {
        deposits(curator: {userId: "archie001"}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
