query {
    depositor(id: "user001") {
        deposits(curator: {userId: "archie001"}, orderBy: {field: DEPOSIT_ID, direction: ASC}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
