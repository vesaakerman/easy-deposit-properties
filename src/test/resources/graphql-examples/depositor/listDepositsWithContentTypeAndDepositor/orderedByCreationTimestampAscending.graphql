query {
    depositor(id: "user001") {
        deposits(contentType: {value: ZIP}, orderBy: {field: CREATION_TIMESTAMP, direction: ASC}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
