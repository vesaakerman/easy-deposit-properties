query {
    depositor(id: "user001") {
        deposits(contentType: {value: ZIP}, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
