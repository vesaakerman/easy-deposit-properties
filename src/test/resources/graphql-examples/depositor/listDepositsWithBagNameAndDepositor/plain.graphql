query {
    depositor(id: "user002") {
        deposits(bagName: "bag3") {
            edges {
                node {
                    depositId
                    bagName
                }
            }
        }
    }
}
