query {
    depositor(id: "user001") {
        deposits(curationRequired: {value: true, filter: LATEST}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
