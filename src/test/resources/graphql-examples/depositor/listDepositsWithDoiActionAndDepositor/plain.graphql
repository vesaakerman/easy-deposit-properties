query {
    depositor(id: "user001") {
        deposits(doiAction: {value: "create", filter: LATEST}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
