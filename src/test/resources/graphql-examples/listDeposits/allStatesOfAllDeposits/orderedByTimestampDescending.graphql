query {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        states(orderBy: {field: TIMESTAMP, direction: DESC}) {
            label
            description
            timestamp
        }
    }
}
