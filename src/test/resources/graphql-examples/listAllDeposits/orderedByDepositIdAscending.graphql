query ListAllDepositsOrderedByDepositIdAscending {
    deposits(orderBy: {field: DEPOSIT_ID, direction: ASC}) {
        id
        depositor {
            depositorId
        }
        state {
            label
            description
        }
    }
}
