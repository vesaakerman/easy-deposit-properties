query ListDepositsWithState {
    depositsWithState(label: SUBMITTED, orderBy: {field: CREATION_TIMESTAMP, direction: DESC}) {
        id
        depositor {
            depositorId
        }
    }
}
