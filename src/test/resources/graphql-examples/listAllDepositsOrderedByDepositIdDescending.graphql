query ListAllDepositsOrderedByDepositIdDescending {
    deposits(orderBy: {field: DEPOSIT_ID, direction: DESC}) {
        id
        depositor {
            depositorId
        }
        state {
            label
            description
        }
    }
}
