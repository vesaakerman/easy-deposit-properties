query {
    deposit(id: "00000000-0000-0000-0000-000000000002") {
        springfields(orderBy: {field: TIMESTAMP, direction: DESC}) {
            domain
            user
            collection
            playmode
            timestamp
        }
    }
}
